let usaPop: string= "1,433,987,286";
let cities: string[]=["cicago", "Newyork", "Dallas"];
export default usaPop;
export {cities}; //we can change the variable name because we havn't use default keyword.